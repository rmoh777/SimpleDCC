// SvelteKit hooks - environment variables are handled by SvelteKit automatically
// In production, environment variables are provided by the platform (Cloudflare, Vercel, etc.)

// SvelteKit hooks can be exported here if needed
// export async function handle({ event, resolve }) {
//   return resolve(event);
// } 